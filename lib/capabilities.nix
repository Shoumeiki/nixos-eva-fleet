# Single source of truth for the fleet's capability names, shared by
# lib/default.nix (mkHost) and modules/core/nerv-options.nix (option decls)
# so the two never drift apart.
[
  "desktop"
  "gaming"
  "virtualisation"
  "server"
  "nas"
  "creative"
]
